# Pinned to exactly what the runtime venv installs (~/.venvs/ctf, Python 3.12).
#
# These pins used to be fiction: nothing on the Mac Mini installed from this
# file, so CI resolved one set (fastapi 0.115.6, pydantic 2.10.4,
# pytest-asyncio 0.25.0) while the service ran another (0.128.8 / 2.13.4 / 1.2.0,
# a major version apart). A green CI run was therefore not evidence the deployed
# code worked. scripts/deploy.sh + the venv close that gap; keep it closed by
# regenerating from `pip freeze` whenever this changes.
fastapi==0.141.1
uvicorn[standard]==0.39.0
pydantic==2.10.4
# starlette arrives via fastapi, but it IS the HTTP layer and carried 7
# advisories (3 HIGH: SSRF/NTLM credential theft, O(n^2) Range DoS) that
# were invisible while it appeared in no manifest. Pinned explicitly so it
# is watched directly.
starlette==1.3.1
httpx==0.28.1
slowapi==0.1.9

# Test-only. pytest 9.0.3 patches GHSA-6w46-j5rx-g56g (vulnerable tmpdir
# handling) and requires Python >=3.10 -- which is why the runtime had to leave
# 3.9 before this could be taken. pytest-asyncio must move with it: 0.25.0
# declares pytest<9.
pytest==9.0.3
pytest-asyncio==1.4.0

# Removed, all with zero imports anywhere in app/:
#   redis, python-jose[cryptography], python-dotenv, pydantic-settings
