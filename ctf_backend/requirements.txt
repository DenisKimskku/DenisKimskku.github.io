# Pinned deliberately: there is no lockfile, and an unattended `pip install -U`
# can change ProxyHeadersMiddleware semantics under the rate limiter.
fastapi==0.115.6
uvicorn[standard]==0.39.0
pydantic==2.10.4
httpx==0.28.1
slowapi==0.1.9
pytest==8.3.4
pytest-asyncio==0.25.0

# Removed, all with zero imports anywhere in app/:
#   redis                     - sessions are SQLite; nothing imports redis
#   pydantic-settings         - config.py is a plain class, not BaseSettings
#   python-dotenv             - never called
#   python-jose[cryptography] - no JWT anywhere. Removed on security grounds as
#                               much as tidiness: an unused JWT library with a
#                               CVE history is the wrong thing to find in a
#                               security showcase's dependency list.
