{
  "name": "deniskim1-ask-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxying the 'Ask AI about this selection' feature for deniskim1.com (keeps the NVIDIA API key server-side)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260822.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.0"
  },
  "overrides": {
    "sharp": "^0.35.3",
    "undici": "^7.29.0"
  }
}
